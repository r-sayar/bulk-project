# Cache (preprocessing, etc.)
.cache/

# Virtual environment
venv/
.venv/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# macOS
.DS_Store

# Processed data (too large for git — regenerate via scripts/)
src/processed_data/

# Uncompressed raw data (use the .gz versions)
data/GSE84133_RAW/*.csv

# Downloaded 10x data (re-download via load_gsm8696075())
data/gsm8696075_10x/

# GEO downloads (re-download via load_geo_accession())
data/GSM*/
data/GSE*/
!data/GSE84133_RAW/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

src/results/
agents/

# venv variants
.venv-drvi/

# Large model checkpoints (regeneratable from src/)
cross_modality_vae.pt
drvi_outputs/

# Pseudobulk + downloaded raw data (large; regeneratable)
pseudobulk/
data/annotations/
data/downloaded_sc/
data/geo_cache/

# Large GENCODE GTFs (regeneratable from EBI mirror)
lncrna_analysis/gencode.*.gtf.gz

# Large all-lncRNA tables (regenerated by scripts; keep the small summaries)
lncrna_analysis/all_lncrnas.csv

# Sample-by-sample matched tables (regeneratable)
matched_bulk_sc.tsv
matched_bulk_sc.test.tsv
matched_bulk_sc.clean.tsv

# Old jsx UI mockup (not part of the analysis)
gene_expression_analysis.jsx